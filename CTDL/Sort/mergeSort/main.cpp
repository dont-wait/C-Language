#include <iostream>
using namespace std;
void mergeSort(int a[]int size);
int main(int argc, char** argv) {
	
	int a[] = {10, 5, 15, 35, 40, 45, 55, 0};
	int size = sizeof(a) / 4;
	return 0;
}
void mergeSort(int a[], int size) {
	int left = 0, right = size - 1;
	int middle = (left + right) / 2; 
	
	
	
}